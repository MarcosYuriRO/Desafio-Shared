package desafio_kindred.src;

import java.util.*;

public class Ovelha extends Animal{
    public void mover() {
        Random gerador = new Random();

        if (status == status.VIVO && turnosPassados == 3) {
            //Gera uma nova ovelha em uma posição livre
        }
    }
}